// Firebase removed. This file kept intentionally empty to avoid broken imports
// if some leftover code still references '../firebase'. All exports replaced
// with no-op placeholders.
export const auth = undefined as unknown as never;
export const db = undefined as unknown as never;
export default {};
